function GameState({ gameState, gameMode }) {
  return (
      <div className='GameState'>
        state = {gameState} <br />
        mode = {gameMode}
      </div>
    )
}

export default GameState;
